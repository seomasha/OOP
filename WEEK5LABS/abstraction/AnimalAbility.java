package OOP.WEEK5LABS.abstraction;

public interface AnimalAbility {
    //variables are static and final meaning they cannot be changed,
    // they are constant the main difference with an abstract class
    //usually have the suffix "able"
    String walk();
}
